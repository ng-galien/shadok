version: 1
project: quarkus-hello
groups:
  quarkus-outputs:
    mode: build
    roots:
      - mount: application
        path: build/shadok-sync
        exclude:
          - lib
          - lib/**
          - app
          - app/**
          - quarkus
          - quarkus/**
          - '*.jar'
          - quarkus-app-dependencies.txt
